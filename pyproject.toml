[project]
name = "aggregator"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.10"
dependencies = [
    "boto3>=1.36.24",
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
]
